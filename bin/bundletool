#!/usr/bin/env bash

set -euo pipefail

readonly bundletool_path="$PWD/bin/bundletool.jar"

if [ ! -f "$bundletool_path" ]; then
  echo "$bundletool_path not found" 1>&2
  exit 1
fi

exec java -jar "$bundletool_path" "$@"